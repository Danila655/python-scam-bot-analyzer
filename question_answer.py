# ЗАДАНИЕ ДЛЯ КОМАНДЫ 2.
# Этот файл отвечает за один игровой раунд, проверку ответа и итоговое звание.
# Здесь нельзя менять data.py и config.py, нужно только пользоваться готовыми данными.
# Проверь себя: после реализации запусти python -m py_compile question_answer.py.
# Ещё проверь себя: функции должны возвращать именно то, что написано в техническом задании.

# Эта строка берёт из файла config.py количество очков за правильный ответ.
from config import POINTS


# Эта функция должна показать игроку один вопрос и текущий статус игры.
def show_question(question_num, case, total, lives, score):
    # Шаг 1: выведи рамку из символов "=".
    # Шаг 2: выведи номер вопроса в формате question_num/total.
    # Шаг 3: выведи количество жизней lives.
    # Шаг 4: выведи текущий счёт score.
    # Шаг 5: выведи слово "Сообщение:".
    # Шаг 6: выведи текст сообщения из case["text"].
    # Проверка: создай тестовый словарь с ключом "text" и вызови show_question(1, case, 20, 3, 0).
    pass


# Эта функция должна спросить игрока, считает ли он сообщение скамом.
def get_choice():
    # Шаг 1: запусти бесконечный цикл while True.
    # Шаг 2: спроси игрока через input(), что он выбирает.
    # Шаг 3: используй .strip(), чтобы убрать лишние пробелы вокруг ответа.
    # Шаг 4: если игрок ввёл "1", верни True.
    # Шаг 5: если игрок ввёл "2", верни False.
    # Шаг 6: если игрок ввёл что-то другое, выведи подсказку и повтори вопрос.
    # Проверка: вызови get_choice() и попробуй ввести 1, 2, пустую строку и неправильный текст.
    pass


# Эта функция должна проверить ответ игрока и обновить жизни, счёт и число правильных ответов.
def process_answer(player, case, lives, score, correct):
    # Шаг 1: сравни player с case["is_scam"].
    # Шаг 2: если ответы совпали, выведи "ПРАВИЛЬНО!".
    # Шаг 3: если ответы совпали, прибавь POINTS к score.
    # Шаг 4: если ответы совпали, прибавь 1 к correct.
    # Шаг 5: если ответы не совпали, выведи "НЕПРАВИЛЬНО!".
    # Шаг 6: если ответы не совпали, уменьши lives на 1.
    # Шаг 7: выведи объяснение из case["explanation"].
    # Шаг 8: верни новые значения в таком порядке: lives, score, correct.
    # Проверка: создай case с is_scam=True и проверь правильный и неправильный ответ.
    pass


# Эта функция должна вернуть итоговое звание игрока по набранным очкам.
def final_title(score, total):
    # Шаг 1: посчитай максимальный счёт как total * POINTS.
    # Шаг 2: посчитай долю результата как score / максимальный счёт.
    # Шаг 3: если доля 0.8 или больше, верни звание кибердетектива.
    # Шаг 4: если доля 0.5 или больше, но меньше 0.8, верни звание осторожного пользователя.
    # Шаг 5: если доля меньше 0.5, верни звание новичка.
    # Проверка: вызови final_title(160, 20), final_title(100, 20), final_title(40, 20).
    pass
